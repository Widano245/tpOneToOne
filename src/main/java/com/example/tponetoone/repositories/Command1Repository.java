package com.example.tponetoone.repositories;

import com.example.tponetoone.entities.Command1;
import org.springframework.data.jpa.repository.JpaRepository;

public interface Command1Repository extends JpaRepository<Command1, Long> {
}
